import 'package:flutter/cupertino.dart';

class MyVideos extends StatelessWidget {
  const MyVideos({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('hello my Videos'),
      ),
    );
  }
}
