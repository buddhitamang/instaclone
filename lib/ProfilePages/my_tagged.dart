import 'package:flutter/cupertino.dart';

class MyTagged extends StatelessWidget {
  const MyTagged({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('hello my Tagged'),
      ),
    );
  }
}
